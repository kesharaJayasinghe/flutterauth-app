import 'package:flutter/material.dart';

class SigninSuccessScreen extends StatelessWidget {
  static String routeName = "/signin_success";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Sign In Successful!"),
    );
  }
}
